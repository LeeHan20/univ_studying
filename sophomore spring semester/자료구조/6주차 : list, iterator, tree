자료구조 6주차

list 
- linked list or array로 구현 가능
- add, set, remove, get 등등
- 니가 아는 그거임

Amortized Analysis
- O(n) notation과 다르게 평균값을 구함
- 모든 것들에 대해 평균을 구하는 분석 방법

dymanic array
- 다른 array를 가져와서 붙이는거
- 크기 가변적인 그거

positional list
- iterator 관리하는 리스트 begin(first), end(last)로 해당 배열에 접근함
- addfirst, addbefore 등등
- r(position).getElement() -> r에 해당하는 원소 리턴
- doubly linked list를 이용하여 구현하는게 자연스러움
- linked list에 direct access가 가능하게 함 -> 메모리를 사용하여

iterator
- 자료구조를 스캔하는 객체
- next, hasnext
- iterable하다 : 순회할 수 있다

tree
- non-linear
- depth of "node" -> for that node
- height of "tree" -> sub tree or tree

preorder
- 나 먼저 보고 자식을 봄
- 나-왼-오
- depth first algorithm
- 왼쪽으로 쭉 가는데 그 순서 그대로임
- 이게 basic인 듯

postorder
- 자식 먼저 보고 나를 봄
- 왼-오-나
- depth first algorithm

inorder
- 자식 하나를 먼저 보고 날 보고 다른 자식을 봄
- 왼-나-오






