chapter 06

# 관계(Relation)
- 두 개 이상의 집합의 원소 사이의 연결을 나타내는 개념
- 순서쌍 : 원소들을 표현하는 방법. 원소 사이에 순서(order)가 존재함을 의미
- 곱집합 : {(a, b) | a ∈ A, b ∈ B} | caution. 행렬과 같이 A*B와 B*A는 다름
    확장 곱집합 : 세 집합 이상에도 적용 가능
    ex. A × B × C = {(a, b, c) | a ∈ A, b ∈ B, c ∈ C}
- 이항관계 : 두 집합 A, B에 대해 집합 A에서 B로 가는 관계
    정의역 : 위에서 A
    공역 : 위에서 B
    치역 : 공역 중에서 A의 element와 관계가 있는 원소
- n항 관계 : n개의 집합에 대한 관계
- 항등관계 : 집합 A에 대해 R = {(a, a)|임의의 a ∈ A}일 때 / 반사관계의 특수한 경우
- 역관계 : {(b, a) ∈ B x A| (a, b) ∈ R} 그냥 순서 바뀐거

# 관계의 표현
- 화살표 선도(arrow diagram) : 화살표로 관계를 나타내는 것
- 좌표도표(coordinate diagram) : 그냥 좌표평면 쓰는거
- 관계행렬(relation matrix) : 부울 행렬의 행과 열에 element를 쓰고 관계가 있으면 1, 없으면 0으로 표기
- 방향 그래프(directed gragh) : A에서 A로 가는 관계를 directed하게 표현

# 관계의 성질
- 반사관계(reflexive relation) : 모든 a ∈ A에 대해 (a, a) ∈ R인 관계
- 비반사관계(irrefelxive relatino) : 모든 a ∈ A에 대해 (a, a) ∈ R이 아닌 관계
- 대칭관계(symmetric relation) : a, b ∈ A에 대해 (a, b) ∈ R이면 (b, a) ∈ R인 관계
- 반대칭관계(antisymmetric relation) : a, b ∈ A에 대해 (a, b) ∈ R이고 (b, a) ∈ R이면 a = b인 관계, 반대는 성립하지 않는 관계
- 추이관계(transitive relation) : a, b, c ∈ A에 대해 (a, b) ∈ R이고, (b, c) ∈ R이면 (a, c) ∈ 관계

# 합성관계
- (composite relation) : A -> B, B -> C일 때, A -> B를 정의하는 것
- 표현 : 부울 행렬로 표기
- 연산 : 두 관계 R과 S를 곱하면 (가운데에 있는 B에 의해 연산 가능) 그게 합성된 결과와 같음
    곱은 AND(∧), 합은 OR(∨) 로 연산하면 됨(사실 뭐 당연)
    거듭제곱 : 말 그대로 그냥 행렬의 거듭제곱임 / R이 추이관계라면 거듭제곱을 계속 해도 R과 같음
- calc properties : 행렬 연산과 동일. (교환, 합성 등)
- 경로의 길이 idea : 그래프에서 몇 번 갔느냐. 즉, 몇 번의 경로를 거쳐갔느냐로 하나의 index로 활용함
    notation. 길이 n의 경로가 존재한다.
    note. I(identity)와의 관계를 길이에 포함하지 않음

# 관계의 폐포
- 폐포(closure) : 이전의 것에 성질을 유지하며 성질을 추가하는 것
- 관계에서의 폐포 : 관계 R이 성질 P를 가질 수 있고 R1이 둘을 포함할 때, R1을 R에 대한 P 폐포라고 한다
- 반사폐포(reflexive closure) : 모든 a에 (a, a) 추가
    만드는 방법 : R ∪ {(a, a)}
- 대칭폐포(symmetric closure) : (a, b)가 있으면 (b, a)도 추가
    만드는 방법 : R ∪ R⁻¹
- 추이폐포(transitive closure) : (a, b), (b, c) 있으면 (a, c) 추가
    만드는 방법 : R ∪ R² ∪ R³ … = R⁺
    경로 : 추이폐포가 pow해서 구하니까 graph 구성할 때 도달할 수 있느냐
    길이 : 몇 번 pow해서 갔느냐
    연결관계 : 길이가 n인 관계 Rⁿ에 대해 xRⁿy는 x에서 y로의 길이가 n인 경로가 존재. n은 상수가 아닌 무한일 수도 있음
    도달관계 : x = y 혹은 lim n -> ∞(xRⁿy)
    note. 유향 그래프에서 두 정점 사이에 도달관계가 성립하면 강하게 연결(strongly connected)되었다고 함

# 동치관계, 부분순서관계
- 동치관계(equivalence relation) : 반사, 대칭, 추이관계가 모두 성립하는 관계
- 동치류(equivalence class) : 동치관계에 있는 원소들의 모임, S = {A₁, A₂, ...}
	prop : i = 1, 2...이면 Aᵢ != ∅
		   Aᵢ ⊆ A
		   i != j이면 Aᵢ ∩ Aⱼ = ∅

		
chapter 07
# 함수의 개념
- f : A -> B에 대해 
- 원상(preimage): b에 대응하는 a
- 상(image) : a에 대응하는 b
- 정의역(domain) : 원상의 집합
- 공역(codomain) : 상이 포합된 집합
- 치역(range) : 상의 집합. B의 부분집합

ex) 7.1
- f : A{a, b, c, d} -> B{1, 2, 3, 4}에 대해 다음이 함수인지 아닌지 판단하여라
-> sol) 1. f₁ = {(a, 4), (b, 1), (c, 3)} 
			-> d에 대응하는 공역 원소가 없음
			∴ 함수가 아님
		2. f₂ = {(a, 3), (b, 4), (c, 2), (d, 2)}
			-> 정의역에 대응하는 모든 원소가 있으며, 정의역의 원소 중복이 없음
			∴ 함수
		3. f₃ = {(a, 1), (a, 3), (b, 2), (c, 2), (d, 4)}
			-> 정의역 원소 중복
			∴ 함수가 아님

ex) 7.2
- 함수인지 아닌지 판별하고 아니라면 정의역을 수정하여 함수로 바꿔라
-> sol) 1. f₁ : ℝ -> ℝ일 때, √(9 - x²)
			함수가 아님. |x| ≥ 3에 해당하는 정의역에 대응하는 값이 공역에 존재하지 않음
			modified domain : {x | |x| ≤ 3, x ∈ ℝ}
		2. f₂ : ℤ -> ℝ일 때, 3 / (5x - 2)
			함수. 분모가 0이 되는 x갑은 5/2 뿐인데, 해당 값은 정수가 아님
		3. f₃ : ℤ - {0} -> ℕ일 때, x²
			함수. 0을 제외한 모든 실수에 대응하는 값이 있으며, x²으로 나오는 모든 값은 ℕ(자연수)임
		4. f₄ : ℕ -> ℕ일 때, x - 3
			함수가 아님. x ≤ 4부터 결과가 음수인데, 공역은 자연수임
			modified domain : {x | x ≥ 3, x ∈ ℕ}

# 함수의 성질
- 단사함수(injective function) : f : X -> Y에 대해 x₁, x₂ ∈ X이고 x₁ != x₂이면 f(x₁) != f(x₂)인 함수, 그냥 일대일대을
	condition : |Y| ≥ |X|
- 전사함수(surjective function) : f : X -> Y에 대해 모든 공역의 원소 y ∈ Y이고 f(x) = y인 정의역 원소 x ∈ X가 적어도 하나 이상 존재하는 함수
	prop : |dom(f)| ≥ |codom(f)|
		   |ran(f)| = |codom(f)|
	condition : |X| ≥ |Y|
- 전단사함수(bijective function) : 단사함수이면서 전사함수인 함수
	prop : |dom(f)| = |codom(f)|
		   |ran(f)| = |codom(f)|
	condition : |Y| = |X|

ex) 7.5
- A = {a | a ≥ 0, a ∈ ℝ}이고, f : ℝ -> A일 때, f(x) = |x|이다. f가 전단사함수인지 파악하시오
-> sol) 1. 단사함수인가? 
			정의역의 0을 제외한 모든 수의 음/양수가 해당 수의 절댓값에 대응되어 단사함수가 아니다.
		2. 전사함수인가?
			|dom(f)| ≥ |codom(f)|이며 |ran(f)| = |codom(f)|이기 때문에 전사함수이다

# 합성함수
- 합성함수(composite function, g ∘ f) : f : A -> B, g : B -> C에 대해 A -> C인 함수
	prop : 교환법칙 성립하지 않음
		   결합법칙 성립
		   f, g 단, 전, 전단사 -> g ∘ f 단, 전, 전단사
		   g ∘ f 단사 -> f 단사
		   g ∘ f 전사 -> g 전사
		   g ∘ f 전단사 -> f 단사, g 전사

# 함수의 종류
- 항등함수(identity function) : f : A -> A에 대해 f(a) = a
- 역함수(inverse function) : 말그대로
	prop : f⁻¹ ∘ f = I
		   (g ∘ f)⁻¹ = f⁻¹ ∘ g⁻¹
- 가역함수(invertible function) : 전단사함수로 역함수가 존재하는 함수. 전단사 ⇔ 가역
- 상수함수(constant function) : 정의역의 모든 원소가 공역의 하나의 원소에만 대응되는 함수
- 특성함수(characteristic function, f_A) : 전체집합 U의 부분집합인 A에 대해 해당 특성으로 나눠지는 함수
	ex : A = {x | x는 알파벳 소문자}
		 f_A = 1, x가 소문자일 떄
			   2, x가 대문자일 떄
- 바닥함수(floor function), 최대정수함수(greatest interger function) : x ∈ ℝ에 대해 x보다 작거나 같은 정수 중 가장 큰 정수를 구하는 함수
	notation : ⌊x⌋
- 천장함수(ceiling function), 최소정수함수(least integer function) : x ∈ ℝ에 대해 x보다 크거나 같은 정수 중 가장 작은 정수를 구하는 함수
	notation : ⌈x⌉


chapter 08

# 부분순서관계
- 부분순서관계(partial order relation) : 반대, 반대칭, 추이관계가 성립하는 관계 
	notation : a ≼ b
	note. 순위를 비교하는 연산자임
- 부분순서집합(... set) : 부분순서관계를 만족하는 집합

ex) 8.1
- 다음이 부분순서관계인가?
-> sol) 1. 정수 집합에 관한 연산 ≤
			a ≤ a가 성립 -> 반사관계
			a ≤ b, b ≤ c라면 a ≤ c가 성립 -> 추이관계
			a ≤ b and b ≤ a라면 b = a -> 반대칭관계
			∴ 부분순서관계이다
		2. 자연수 집합에 관한 연산 x | y
			x | x가 성립 -> 반사관계
			x | y, y | z라면 x | z가 성립 -> 추이관계
			x | y, y | x라면 x = y가 성립 -> 반대칭관계
			∴ 부분순서관계이다
		3. 자연수 집합에 관한 연산 <
			a < a가 성립하지 않음
			∴ 부분순서관계가 아니다
		4. A = {1}일 때, A의 멱집합에 관한 관계 ⊆
			멱집합 : P = {∅, {1}}
			p의 임의의 원소 a, b에 대해
			a ⊆ a가 성립 -> 반사관계
			∅ ⊆ {1}, {1} ⊆ {1}이면 ∅ ⊆ {1}이라서 추이관계...? -> 이게 무슨 ㅋㅋㅋ
			a ⊆ b, b ⊆ a라면 a는 b임 -> 반대칭관계

- 부분순서관계 by 부울 행렬
	ex. A = {1, 2, 3}, 동치관계 R = {(1, 1), (1, 2), (1, 3), (2, 2), (3, 3)}
			| 1 1 1 |
		M = | 0 1 0 |
			| 0 0 1 |
	prop : 반사관계 : 주대각 원소가 모두 1
		   반대칭관계 : 주대각 원소를 기준으로 관계 R의 원소와 마주보는 원소 중 하나만 1일 수 있음
		   추이관계 : 제곱(부울 곱)하였을 때 값이 똑같음. -> 가는 경로의 관점
- 비교 가능(comparable)과 비교 불가능(noncomparable) : 부분순서집합인 A의 원소 a, b에 대해 a ≼ b or b ≼ a라면 비교 가능, 아니라면 불가능
- 전순서관계(total order relation) : 집합 A에 대한 관계 R이 부분순서관계일 때, A의 모든 원소가 R에서 비교 가능할 때의 R		// 완전순서관계
- 전순서집합(... set) : 완전순서관계를 만족하는 집합

ex) 8.3
- 집합 A = {1, 2, 3}에 대해 (P(A), ⊆)가 전순서집합인가?
-> sol) 멱집합 = {∅, {1}, {2}, {3}, {1, 2}, {2, 3}, {1, 3}}
		부분집합 관계가 성립하지 않는 원소들({1},{2},{3}...이 존재.
		∴ 전순서집합이 아님

- 곱 부분순서관계(product partial ordered relation) :  (A, ≼), (B, ≼)가 부분순서집합일 때, (A * B, ≼)도 부분순서집합인데, 이 때의 ≼.
- 곱 부분순서집합(... set) : 위의 (A * B, ≼)
- 사전직 순서(lexicographic order) : 곱 부분순서집합의 부분순서집합
	notation : (a, b) ≺ (a', b')
- 하세 도형(Hasse diagram) : 각 원소의 순서관계를 그래프로 표현한 것
	유향 그래프 변환 방법 : 1. 길이가 1인 순회 제거(자기 자신)
					   2. 추이관계에 의해 만들어진 간선 제거
					   3. 사이클이 없다면 세로로 정렬시킨다
					   4. 간선을 작은 점으로 바꾼다

ex) 8.5
- A = {1, 2, 3, 4, 12}이고, A에 관한 부분순서관계를 다음과 같이 정의할 때, 부분순서집합 (A, ≼)의 하세 도형은?
	a, b ∈ A에 대해 a ≼ b ⇔ a | b (b가 a로 나눠떨어짐)
-> sol) 1. 관계 R 계산
		    R = {(1, 1), (1, 2), (1, 3), (1, 4), (1, 12), (2, 2), (2, 4), (2, 12), (3, 3), (3, 12), (4, 4), (4, 12), (12, 12)}
		2. R이 부분순서관계인가?
			모든 원소에 대해 (a, a)꼴이 있음 -> 반사
			모든 원소에 대해 (a, b)이면 (b, a)인 꼴이 없음 -> 반대칭
			모든 원소에 대해 (a, b), (b, c)이면 (a, c)가 있음 -> 추이
		3. 하세 도형 그리기
			https://csacademy.com/app/graph_editor/
			여기에서 그리면 됨

ex) 8.6
- A = {1, 2}의 멱집합 P에 대해 (P, ⊆)가 부분순서집합인지 보이고 부분순서집합이라면 하세 도형
-> sol) 1. P = {∅, {1}, {2}, {1, 2}}
		2. 위의 포함된계를 만족하는 관계 R 구하기
			R = {(∅, ∅), (∅, {1}), (∅, {2}), (∅, {1, 2}), 
				 ({1}, {1}), ({1}, {1, 2})
				 ({2}, {2}), ({2}, {1, 2})
				 ({1, 2}, {1, 2})}
		3. ⊆의 성질에 의해 반사, 추이가 성립하며 집합의 성질에 의해 비반사관계가 성립
		4. 허세 도형 그리기

- 위상정렬(topological sorting) : 부분순서집합이 주어질 때, 부분순서집합으로부터 전순서관계를 찾는 것. 즉 order하는것.

# 상한과 하한
- 극대 원소(maximal element), 극소 원소(minimal element) : (A, ≼)가 부분순서집합일 떄, 
	a ∈ A에 대해 a < c인 집합 c가 집합 A에 존재하지 않으면 a가 극대. 극소도 마찬가지

ex) 8.9
- 하세 도형이 주어졌을 때, 극대 원소와 극소 원소를 구하여라

					  a3∘
					   / \
    a1∘		a2∘       ∘   ∘
	   \     / \     /     \
	    ∘	∘   ∘   ∘       ∘
	     \ /     \ /       /
		b1∘     b2∘       ∘
						/
				     b3∘

-> sol) 말 그대로 최소/최대가 아닌 극대/극소이기 때문에 a₁, a₂, a₃이 극대, b₁, b₂, b₃이 모두 극값
		또한 극값들은 각각 간선을 통해 연결되어 있지 않기 때문에 뭐가 더 크거나 작은 원소인지 판단할 수 없음

- 최대 원소(greatest element), 최소 원소(least element) : 너가 아는 그거 맞음
	note. 만약 최대에 해당하는 값이 두 개가 있으면 최대 원소가 없는 것으로 취급
- 단위원 : 최대 원소, I로 표기
- 영원(zero element) : 최소 원소, 0으로 표기
- 상계(upper bound), 하계(lower bound) : A가 부분순서집합, B는 A의 부분집합이고 a ∈ A일 떄, 
	B의 모든 원소에 대해 b ≤ a라면 a를 B의 상계라고 하고 a ≤ b이면 하계라고 함
- 상한(least upper bound, superemum), 하한(greatest lower bound, infimum) : 상계 중 가장 작은거, 하계 중 가장 큰거
	notation : LUB(B), GLB(B)
	prop : 최대 원소와 같이 유일함
	cf. LUB({a, b})를 a와 b의 논리합, a ∨ b (join)
		GLB({a, b})를 a와 b의 논리곱, a ∧ b (meet)로 부르기도 함
		-> or, and 하면 상/하한이 나오기 때문

ex) 8.16
- 다음 하세 도형에 대해 B₁ = {2, 5, 6}, B₂ = {6, 7, 8}의 LUB, GLB를 구하시오

			11
           /   \
         9      10
        /  \   / | \
       5     6   7  8
        \  / | \ | /
         2   3   4 
          \  |  /
             1

-> sol) B₁의 경우 상계는 6보다 큰 위상이 있는 9, 10, 11, 하계는 2보다 작은 위상이 있는 1이다.
		B₂도 마찬가지로 판단

# 격자
- 격자(lattice) : L의 임의의 두 원소 a, b에 대해 집합 {a, b}의 하한과 상한이 각각 하나씩 존재하는 부분순서집합(L, ≼)
	prop : 멱등, 교환, 결합, 흡수법칙 성립
	
ex) 8.19
- 다음 하세 도형이 격자인가
-> sol) 그냥 모든 두 개의 점에 대해 공통되는 상/하한이 무조건 1개 있어야 함
	∘		
   / \			
  ∘   ∘
  | x |
  ∘   ∘
   \ /
    ∘
-> 아님

- 부분격자(sublattice) : 격자의 성질을 유지하는 격자의 부분집합
- 완비격자(complete lattice) : 모든 부분집합이 부분격자인 격자
- 유계격자(bounded lattice) : 최대/최소가 모두 존재하는 격자
- 분배격자(distributive lattice) : 원소들끼리 분배법칙이 성립하는 격자
- 여원(complement element) : 최소 원소 0, 최대 원소 I에 대해 a ∧ a' = 0, a ∨ a' = I일 때, a'이 a의 여원
- 여격자(complement lattice) : 모든 원소가 하나의 여원을 가지는 유계격자
-> 그냥 외워..?

# 부울 대수
- 부울 대수(boolean algebra) : 적어도 두 개의 원소를 가지는 여격자인 동시에 분배격자인 격자
	1. 격자 -> and, or 연산 정의됨
	2. 여격자 : 모든 원소에 not 존재
	3. 분배격자 : and와 or이 분배법칙 만족

- 알잖아

# 가산기
- 2비트 가산기 : 4개의 2진수 입력에 대해 3개의 출력을 내 3비트 수를 만듦
- 반가산기(half adder) : 두 개의 1비트 입력에 대해 합과 carry 출력
	carry : xy, 합 :  x'y ∨ xy'


1 - | 반가산기 | ----------- |or| - 1
0   |	     | \		/
  x				|반가산기| -------- 0
1   | 반가산기 | /
1 - |        | ------------------ 1


chapter 09

# 그래프
- 단순그래프 : vertex 사이에 edge 하나
- 다중그래프 : vertex 사이에 edge 두 개 이상
- 외차수 : 여기에서 나가는 edge 수
- 내차수 : 여기로 들어오는 edge 수
- 동형그래프 : 말 그대로 같은거
- 평면그래프 : 연결 구조는 놔두고 아무리 조작해도 교차를 풀 수 없는 그래프
- 오일러 공식(euler's formula) : vertex, edge, space. at 평면그래프
	v - e + s = 2
- 정규그래프(regular graph) : 모든 정점의 차수가 k로 같은 그래프
- 완전그래프(complete graph) : 모든 정점 사이에 변이 존재하는 그래프
- 완전이분그래프(complete bipartite graph) : 이분그래프에서 왼쪽의 모든 점에 대해 모든 오른쪽 점이 연결된 그래프

# 순환
- 오일러 경로 : 모든 edge를 한 번씩 지나는 경로
- 오일러 순환 : 모든 edge 방문 후 돌아오기
- 해밀턴 경로 : 모든 vertex를 한 번씩 지나는 경로
- 해밀턴 순환 : 모든 vertex 방문 후 돌아오기

# 그래프 활용, 스패닝 그래프
- 알잖아


chapter 10

# 트리
- 편향이진트리(skewed binary tree) : 멍청한 그래프. 그냥 한 방향으로 가는 백터
- known

# 순회
- 전위, 중위, 후위
- https://github.com/LeeHan20/univ_studying/tree/main/sophomore%20spring%20semester/%EC%9E%90%EB%A3%8C%EA%B5%AC%EC%A1%B0

# 활용
- 프림, 크루스칼, 다익스트라, 허프만 복호화...
- https://github.com/LeeHan20/univ_studying/tree/main/sophomore%20autumn%20semester/algorithm







	​

	​
