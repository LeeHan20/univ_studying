시스템프로그래밍기초 9주차

data/heap/stack
- 전역변수는 data
- 동적 할당은 heap
- 함수는 stack

cf. 함수는 변수를 사용할 때 먼저 본인의 sf를 확인하고 없으면 data를 확인함

auto 변수
- 일반적으로 int name하면 auto변수임

static 변수
- 함수 안에서 정의하지만 data에 만들어짐
- but, 정의된 함수 내에서만 사용 가능
- 함수가 종료되어도 값이 사라지지 않음
-> 다음 번 함수 호출에서 전 함수가 내놓은 값을 사용할 수 있음
